# v0.6 Nacos

## 目标

引入 Nacos，用于服务注册发现和配置管理，让网关和后端服务从静态地址转向服务名路由。

## 新增能力

服务注册与配置中心。

## 技术范围

| 类别 | 技术 |
|---|---|
| 注册中心 | Nacos |
| 配置中心 | Nacos Config |
| 服务治理 | Spring Cloud |

## 架构变化

```text
Gateway
  -> Nacos service discovery
    -> java-demo-app
    -> task-service
    -> notification-service
```

本阶段优先实现 Nacos 单节点可运行，再保留集群扩展 compose 计划。集群化可以在后续部署阶段继续强化。

`v0.5.1` 已规划任务和通知两个业务服务，因此本阶段不只验证单个后端注册，而是让 Gateway、用户、任务、通知服务都进入注册发现体系。这样后续服务扩容、下线和路由切换可以直接在真实业务链路中观察。

`v0.5.2` 已规划后端运行日志基线，因此本阶段接入 Nacos 时需要继续保留并强化关键日志，重点观察服务注册、配置读取、服务名调用、配置刷新和服务不可用场景。

`v0.5.3` 已规划 React/Vue 任务和通知前端，因此本阶段除接口脚本外，还需要回归两套前端经 Gateway 访问用户、任务和通知功能是否正常。Nacos 改造本身通常不新增页面，但服务发现路由变化不能破坏已有前端操作路径。

## 容器策略

遵守 `docs/decisions/0010-docker-service-containerization.md`。

| 阶段 | 容器要求 |
|---|---|
| 单节点验证 | 使用 Docker Desktop 拉取 Nacos 镜像并运行 `java-demo-nacos-1` 独立容器 |
| 集群扩展 | Nacos 每个节点独立容器，例如 `java-demo-nacos-1`、`java-demo-nacos-2`、`java-demo-nacos-3` |
| 依赖服务 | 如果 Nacos 集群需要数据库，数据库继续使用独立 MySQL 容器，不与 Nacos 合并 |

## 开发步骤

1. 新增 Nacos Docker Compose 文件，单节点容器和后续集群容器规划分开记录。
2. `java-demo-app`、`task-service`、`notification-service` 和 `gateway` 接入服务注册。
3. Gateway 从静态地址路由改为服务名路由，例如用户、任务、通知分别按服务名转发。
4. `task-service` 调用 `java-demo-app`、`task-service` 调用 `notification-service` 的链路从静态地址迁移为服务发现调用。
5. 把部分配置迁移到 Nacos Config，例如 JWT 配置、任务状态开关、通知发送开关或登录限制。
6. 在服务注册、配置加载、配置刷新、服务名调用成功和调用失败处补充必要日志。
7. 编写本地启动顺序说明。
8. 验证服务上下线对网关路由和服务间调用的影响。
9. 回归 React 和 Vue 的登录、任务管理、通知中心，确认前端经 Gateway 调用服务发现路由正常。
10. 准备 Nacos 集群化草案，但不强行复杂化本阶段。
11. 更新 README 和 `docs/PROGRESS.md`。

## 验收标准

1. Nacos 可以启动并访问控制台。
2. Gateway、`java-demo-app`、`task-service`、`notification-service` 可以注册到 Nacos。
3. Gateway 可以通过服务名转发用户、任务和通知请求。
4. `task-service` 可以通过服务发现调用用户服务和通知服务。
5. 修改 Nacos 配置后，服务可以读取到配置。
6. 控制台和文件日志可以观察服务注册、配置读取、服务名调用和失败原因。
7. 登录、用户管理、任务、通知、React、Vue 功能未回归。
8. React 和 Vue 任务/通知页面仍可经 Gateway 正常访问后端。
9. Nacos 容器名称、镜像版本、端口、volume、network 和健康检查已记录。

## 完成定义

1. Nacos 服务注册可用。
2. Nacos 配置读取可用。
3. README 记录 Nacos 启动和访问方式。
4. Nacos 相关关键流程日志可在控制台和文件中观察，且不打印敏感信息。
5. 新增和修改代码已补充详细中文注释。
6. 服务间调用仍可保留当前 HTTP/REST 方式，本版本不同时引入 OpenFeign 或 Dubbo，避免范围过大。
7. 如果需要保存版本，由用户手动提交并手动打 tag：`v0.6-nacos`；Codex 不自动执行提交、tag 或 push。

## 实际实现记录

| 项目 | 实际结果 |
|---|---|
| Nacos Docker Compose | 已新增 `infra/docker-compose/nacos/docker-compose.yml`，单节点容器名为 `java-demo-nacos-1`，端口为 `8848`，并补充 healthcheck、volume 和 network 说明 |
| Nacos 配置导入 | 已新增 `infra/docker-compose/nacos/import-configs.ps1`，负责将 `java-demo-common.yml`、`java-demo-app.yml`、`java-demo-gateway.yml`、`task-service.yml`、`notification-service.yml` 导入 `DEFAULT_GROUP` |
| 服务注册 | `java-demo-app`、Gateway、`task-service`、`notification-service` 已全部接入 Spring Cloud Alibaba Nacos Discovery，并向同一个 Nacos 实例注册 |
| 配置中心 | 四个后端服务已接入 Nacos Config，运行时健康检查与启动日志均可观察 `configSource=nacos`、`configLabel=v0.6-default` |
| Gateway 路由 | Gateway 默认路由已从固定 URI 切换为 `lb://java-demo-app`、`lb://task-service`、`lb://notification-service`，同时保留环境变量覆盖能力用于排查 |
| 服务间调用 | `task-service` 的用户校验与通知创建链路已支持通过服务名调用 `java-demo-app` 和 `notification-service`，测试场景仍可回退到固定 URL，避免影响 H2 集成测试 |
| 日志补充 | 四个服务均已补充 Nacos 配置加载、配置刷新、服务发现和关键启动摘要日志，不打印密码、完整 JWT、Authorization 或真实密钥 |
| 前端联动判断 | 本版本只改造基础设施接入，不新增用户可见能力，因此 React/Vue 无需页面代码改动；仅执行构建回归确认 Gateway 路由切换未破坏现有前端路径 |

## 实际验证结果

| 验证项 | 实际结果 |
|---|---|
| Nacos 容器 | 已执行 `docker compose -f infra\docker-compose\nacos\docker-compose.yml up -d`，`java-demo-nacos-1` 为 `healthy`，控制台可经 `http://localhost:8848/nacos` 访问 |
| 配置导入 | 已执行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\infra\docker-compose\nacos\import-configs.ps1`，五份配置均导入成功 |
| 后端测试 | 已执行 `.\mvnw.cmd test`，通过 |
| 后端打包 | 已执行 `.\mvnw.cmd -DskipTests package`，通过；已生成四个 `0.6.0-SNAPSHOT` 可执行 jar |
| 四服务临时启动 | 已使用 `8252-8255` 启动 `java-demo-app`、Gateway、`task-service`、`notification-service`，避开保留端口段与用户现有进程 |
| 服务注册发现 | Nacos 实例列表可见 `java-demo-app`、`java-demo-gateway`、`task-service`、`notification-service` 各 1 个实例 |
| 配置中心生效 | 直连与经 Gateway 访问健康检查均返回 `configSource=nacos` 和 `configLabel=v0.6-default`，服务角色分别为 `user-service`、`task-service`、`notification-service` |
| 真实 Gateway 联调 | 已经 Gateway 完成注册、拼图 challenge、任务创建、任务状态从 `TODO` 变更为 `IN_PROGRESS`、通知分页和未读数验证；验证用户为 `v060_20260603173509` |
| 前端回归 | `frontend-react` 和 `frontend-vue` 的 `npm.cmd run build` 均通过；保留既有 chunk size warning 与 VueUse 注释提示 |
| 进程清理 | 验证结束后已停止本次临时启动的 Java 进程，`8252-8255` 无监听进程 |

## 已知约束

1. 当前 `spring-alibaba-nacos-config 2023.0.3.2` 在 Windows 上解析 YAML 时，会把 Nacos 返回的字符串按平台默认编码重新转回字节，再交给 YAML 解析器读取。
2. 一旦待发布 YAML 中包含中文注释或其他非 ASCII 字符，Windows 默认编码与 UTF-8 重组后的字节可能不一致，最终触发 `MalformedInputException`。
3. 因此 `infra/docker-compose/nacos/configs/*.yml` 需要保持 ASCII-only；中文说明应写在 README、milestone 或 `docs/PROGRESS.md`，导入脚本也已主动拒绝非 ASCII 内容。

## 后续版本衔接

| 后续版本 | 衔接说明 |
|---|---|
| `v0.6.1 OpenFeign` | 在 Nacos 服务发现可用后，把 `task-service -> java-demo-app` 和 `task-service -> notification-service` 改为 Feign 声明式 HTTP 调用 |
| `v0.6.2 Dubbo RPC` | 在 Feign 改造完成后，只把 `task-service -> java-demo-app` 用户校验链路改为 Dubbo RPC |
| `v0.7 Redis` | 在服务调用方式稳定后，再验证用户校验、任务列表、通知未读数缓存和限流 |
